// ⚠️ À compléter après avoir créé ton projet Firebase.
// Ces valeurs viennent de : Console Firebase → ⚙️ Paramètres du projet
// → onglet "Général" → section "Vos applications" → application Web (icône </>).
// Ce ne sont PAS des secrets : elles sont prévues pour être visibles
// dans le code d'une appli web (la sécurité vient des règles Firestore,
// pas de la confidentialité de ces valeurs).

export const firebaseConfig = {
  apiKey: "REMPLACE_MOI",
  authDomain: "REMPLACE_MOI.firebaseapp.com",
  projectId: "REMPLACE_MOI",
  storageBucket: "REMPLACE_MOI.appspot.com",
  messagingSenderId: "REMPLACE_MOI",
  appId: "REMPLACE_MOI"
};
